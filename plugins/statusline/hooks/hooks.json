{
  "//": "SessionStart runs the plugin's own installer in sync mode: a deterministic script,",
  "//1": "idempotent (an identical install is left untouched and prints nothing), that migrates",
  "//2": "a pre-#33 flat install once and never touches statusline-budget.json or",
  "//3": "ctxguard-thresholds.json. It does nothing at all where no statusline is installed.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/install.sh\" sync",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
